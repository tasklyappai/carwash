# Gleam — Automotive Detailing

A modern car detailing landing page built with React, Vite, Tailwind CSS, and Framer Motion.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
